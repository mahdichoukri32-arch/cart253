/**
 * The Window
 * Mariam-Choukri Mahdi 
 * 
 * It's the boredom of the average human.
 * Who zone out in attempt to entertain himself. 
 * 
 * The program is non-interactive to represent the non existent of dreams. 
 */

"use strict";

/**
 * Creates the base of our dreams
*/
function setup() {
    // Create the canvas with wider resolution than the norm 
    createCanvas(540, 780);
}


/**
 * Draw the person's window and the piece imagination in the window's refelction
*/
function draw() {
    // The purple room
    background(200, 155, 255);
    // The window
    rect(150, 200, 240, 320); 
}